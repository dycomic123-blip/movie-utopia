# 云端部署指南

本指南将帮助您将 Movie Utopia 网站部署到云端。我们提供了多种部署方案，您可以选择最适合您的平台。

## 📋 部署前准备

### 1. 确保项目可以正常构建
```bash
npm run build
```

### 2. 准备环境变量（如果需要）
如果项目需要 GEMINI_API_KEY，请准备好您的 API 密钥。

---

## 🚀 方案一：Vercel（推荐 - 最简单）

Vercel 是部署 React/Vite 项目的最佳选择，提供免费套餐，自动 HTTPS 和全球 CDN。

### 步骤：

1. **注册/登录 Vercel**
   - 访问：https://vercel.com
   - 使用 GitHub、GitLab 或 Bitbucket 账号登录

2. **连接 Git 仓库**
   - 将代码推送到 GitHub/GitLab/Bitbucket
   - 在 Vercel 中点击 "New Project"
   - 导入您的仓库

3. **配置项目**
   - Framework Preset: 选择 "Vite"
   - Root Directory: `./`（默认）
   - Build Command: `npm run build`（自动检测）
   - Output Directory: `dist`（自动检测）

4. **设置环境变量（如果需要）**
   - 在项目设置中添加 `GEMINI_API_KEY`
   - 值：您的 API 密钥

5. **部署**
   - 点击 "Deploy"
   - 等待几分钟，部署完成后会获得一个 URL（如：`your-project.vercel.app`）

### 优势：
- ✅ 完全免费（个人项目）
- ✅ 自动 HTTPS
- ✅ 全球 CDN 加速
- ✅ 自动部署（每次 Git push）
- ✅ 预览部署（PR 自动创建预览）

---

## 🌐 方案二：Netlify

Netlify 也是一个优秀的静态网站托管平台。

### 步骤：

1. **注册/登录 Netlify**
   - 访问：https://www.netlify.com
   - 使用 GitHub 账号登录

2. **部署方式 A：通过 Git**
   - 点击 "Add new site" → "Import an existing project"
   - 连接您的 Git 仓库
   - Build command: `npm run build`
   - Publish directory: `dist`
   - 点击 "Deploy site"

3. **部署方式 B：拖拽部署**
   - 运行 `npm run build`
   - 将 `dist` 文件夹拖拽到 Netlify 的部署区域

4. **设置环境变量（如果需要）**
   - Site settings → Environment variables
   - 添加 `GEMINI_API_KEY`

### 优势：
- ✅ 免费套餐
- ✅ 自动 HTTPS
- ✅ 表单处理功能
- ✅ 边缘函数支持

---

## ⚡ 方案三：Cloudflare Pages

Cloudflare Pages 提供快速的全球 CDN 和免费 SSL。

### 步骤：

1. **注册/登录 Cloudflare**
   - 访问：https://pages.cloudflare.com
   - 使用 Cloudflare 账号登录

2. **连接 Git 仓库**
   - 点击 "Create a project"
   - 连接 GitHub/GitLab
   - 选择您的仓库

3. **配置构建**
   - Framework preset: `Vite`
   - Build command: `npm run build`
   - Build output directory: `dist`

4. **设置环境变量（如果需要）**
   - Settings → Environment variables
   - 添加 `GEMINI_API_KEY`

5. **部署**
   - 点击 "Save and Deploy"

### 优势：
- ✅ 完全免费
- ✅ 极快的全球 CDN
- ✅ 无限带宽
- ✅ 自动 HTTPS

---

## 🐙 方案四：GitHub Pages

适合开源项目，完全免费但需要手动配置。

### 步骤：

1. **安装 gh-pages**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **更新 package.json**
   在 `scripts` 中添加：
   ```json
   "predeploy": "npm run build",
   "deploy": "gh-pages -d dist"
   ```

3. **配置 vite.config.ts**
   添加 `base` 配置（如果仓库名不是根路径）：
   ```typescript
   base: '/movie-utopia/'
   ```

4. **部署**
   ```bash
   npm run deploy
   ```

5. **启用 GitHub Pages**
   - 仓库 Settings → Pages
   - Source: 选择 `gh-pages` 分支
   - 保存后访问：`https://yourusername.github.io/movie-utopia`

---

## 🔧 通用配置说明

### 环境变量配置

如果项目需要环境变量，在各平台设置：

- **Vercel**: Project Settings → Environment Variables
- **Netlify**: Site settings → Environment variables
- **Cloudflare Pages**: Settings → Environment variables

### 自定义域名

所有平台都支持自定义域名：
1. 在平台设置中添加您的域名
2. 按照提示配置 DNS 记录
3. 等待 SSL 证书自动配置（通常几分钟）

---

## 📝 部署检查清单

- [ ] 项目可以正常构建（`npm run build`）
- [ ] 已创建 Git 仓库并推送代码
- [ ] 已选择部署平台
- [ ] 已配置环境变量（如需要）
- [ ] 已测试部署后的网站功能
- [ ] 已配置自定义域名（可选）

---

## 🆘 常见问题

### 构建失败
- 检查 Node.js 版本（建议 18+）
- 确保所有依赖已安装
- 查看构建日志中的错误信息

### 页面 404 错误
- 确保配置了 SPA 路由重定向（所有路由指向 index.html）
- 检查 `_redirects` 或 `vercel.json` 配置

### 环境变量不生效
- 确保在平台设置中正确配置
- 重新部署项目
- 检查变量名是否拼写正确

---

## 🎉 部署完成后

部署成功后，您将获得：
- 🌐 一个公开访问的 URL
- 🔒 自动 HTTPS 证书
- ⚡ 全球 CDN 加速
- 📊 访问统计（部分平台）

祝您部署顺利！
